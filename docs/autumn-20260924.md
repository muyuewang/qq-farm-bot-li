# 2026-09-24 活动与 SVIP 商城更新

## 范围

`main` 与 `master` 分别接入秋祈良愿、快乐不独享两个独立页面，以及游戏商城的 SVIP 独立分页。仅共享本次活动、商城协议、版本、配置和小图标；未合并两条分支的调度实现，也未把 main 的萌宠日记页面或大图资源移植到 master。

游戏版本：`1.14.2.11_20260922`；项目版本：`20260924`。TSDK：`v3.9.0.1790160550`，WASM 为 161,081 bytes，SHA-256：

```text
2c9e377ecc9a4fd9019f12191b589d543a60d6654580eb3e237b35f1fa5b1cb7
```

## 协议依据

来源为官方 QQ 小游戏包及代理会话 `session-1790215551955`、`session-1790221566956`，按切换账号前后区分样本。校验使用解密后的完整请求与响应字段、官方 protobuf 编码向量，没有以响应长度代替内容比较。仓库测试夹具仅保留业务响应、合成向量与明确标记的脱敏回包；快乐日志移除了 Ark token，角色资料替换为测试名称，不包含登录凭据。

| 功能 | 已确认映射 |
| --- | --- |
| 秋祈良愿 | 分组 2026092400、活动 2026092401；状态 field 119；抽签 op 51 / field 151；领奖 op 52 / field 152 |
| 快乐不独享 | 分组 2026092500、活动 2026092501；状态 field 120；分享 op 69、档位领奖 op 70、日志 op 71、每日奖励 op 73 |
| 会员资格 | `GetQQVipRewardsStatusReply.is_qq_vip`（field 1）是官方页面使用的资格标志；field 2 是 `can_claim`，不是第二种会员资格 |
| 商城分页 | 普通商城 slot 1，SVIP slot 4；查询请求 field 2 是 `is_manual_open`；商品 field 8 是可用状态，field 12 是商品类型 |
| 购买确认 | `PurchaseResponse` field 2 为 `success`，不是购买数量；后台重新查询资格、余额、价格和限购状态 |

SVIP 商品 1053 为免费化肥，1054 价格为 110 钻石，1055、1056 分别使用 1901、1902 碎片兑换。非会员可以浏览该分页，前后端均禁止购买。会员自动礼包流程仅领取满足免费条件的商品。

祈愿领奖使用服务端待领奖状态的 `choose_id`。快乐分享操作只调用活动 RPC，每日首次奖励后禁用按钮；不发送 QQ 消息，不代替他人点击，也不调用需要 Ark 点击上下文的 op 72。

快乐日志“我领取的”使用 tab 1，“领取我的”使用 tab 0，与实际请求和官方页面一致。每日领取与分享分别对应日志 kind 4、3；网页仅接收所需的日志及奖励字段，不返回 Ark 分享凭据。

桂月华庭礼包 1060 的原价为 880 钻石，实际回包的限时价格为 780，促销时间为 `1790179200 <= serverTime < 1790783999`。页面显示当前价格及划线原价，后端按服务器时间重新校验价格；提交的确认价格与最新价格不一致时返回 `MALL_PRICE_CHANGED`，不会按过期报价直接购买。验证仅使用回包和模拟购买，未实际消费钻石。

两个活动入口使用官方小图标，总计 28,692 bytes；来源和哈希见 `core/src/activity-data/autumn-assets.json`。页面主体使用 CSS，新增物品使用小图标。

两个新页面的操作通知复用项目 toast，成功领取和操作失败使用浮动提示；刷新与读取日志成功时不显示通知，不插入撑开页面的提示条。

## 验证结果

- 实际客户端：会员账号农耕祈愿（choose 5）帧 561/562、569/570 确认签文“顺应天时”；切换非会员后，前程祈愿（choose 3）帧 1413/1414、1419/1420 确认签文“这件事 / 主要看你”。两次首日奖励均为“烟花·玉兔望月 ×20”，领奖后待领取状态清空；未据此推断所有签文或日期的奖励。
- SVIP：会员帧 327 与非会员帧 1376 的商品完整内容逐项比较，均返回 1053–1056 四项商品，差异仅为各商品 `is_available` 由 true 变为 false；非会员状态帧 1366 的 `is_qq_vip` 为 false。实现允许浏览，前后端禁止购买。
- 快乐不独享：用户完成每日领取和假分享后，新会话帧 115 确认快乐值 10、两项每日标志为 true、首档化肥（4小时）×1 为已领取。帧 130/131 的日志确认两笔各 +5，帧 136/137 确认“领取我的”当前为空；原生页面逐项一致。领取时代理连接中断，未保存当时 op 69/73 的直接回包；结果以恢复连接后的服务端状态和历史记录交叉验证。
- 新 WASM 与两个账号样本的一次性凭据分别达到 112/112 字节一致，初始化和加解密往返通过。
- 浏览器使用模拟 API：两条分支的独立活动入口、抽签/领奖、每日快乐值、首次分享、档位解锁/领奖、会员状态切换、商城 tab 切换、余额不足、弹窗关闭已验证。320、390 和桌面宽度检查没有横向溢出、缺图；修复了活动图标居中、非会员按钮挤压、购买关闭按钮尺寸及未知余额禁用问题。
- 补充浏览器模拟验收：已领取状态禁用重复领取，日志两分页和记录类型正确；320 窄屏显示 780 现价及 880 划线原价，确认购买携带原报价，价格变化时显示错误并保留弹窗。模拟环境没有连接真实购买接口。
- main：284 项 core 测试、18 项 web 测试通过。master 以远程 `8f8fe8b` 为基线重新适配后，184 项 core 测试、4 项 web 测试通过；两条分支的 core/client TypeScript、Vue 类型检查与 Vite 生产构建通过。两边均包含本次调整的活动页面样式。

master 发布分支为 `feat/autumn-activities-svip`，只迁移本次活动、商城与版本资源改动。保留上游现有的萌宠日记页面、协议处理及调度实现，没有合并本地旧 master 的其他历史提交。

## 自动化任务（本地新增）

上游的两个活动页面只有手动操作，`core/src/services/autumn-activities-auto.ts` 为本地补充的自动化模块，按 `pet-diary-auto.ts` 的形状实现（`AUTUMN_AUTOMATION_KEYS` + `isAutumnAutomationEnabled` + `createAutumnAutomation`）。开关在「设置 → 自动控制 → 秋日祈愿活动」，逐项对应上文已解密验证的 operate_type：

| 开关 | 动作 | 触发条件（服务端状态） |
| --- | --- | --- |
| `autumn_wish_claim` | 秋祈良愿领奖（op 52） | `wish_sign.pending.text_id > 0` |
| `autumn_wish_draw` | 秋祈良愿抽签（op 51） | `can_draw` 且今日未抽过，方向取 `autumn_wish_choice` |
| `autumn_happy_daily` | 每日快乐值（op 73） | `summary.daily.daily_reward_claimed == false` |
| `autumn_happy_share` | 首次分享奖励（op 69） | `summary.daily.first_share_awarded == false` |
| `autumn_happy_milestones` | 档位领奖（op 70） | 存在 `state == 2` 的档位 |

日志查询（op 71）只读不改状态，没有为它建开关；没有协议证据的动作（op 72 需要 Ark 点击上下文）不预建键位。判断全部以本轮 `GetGroup` 回包为准，动作之间彼此独立：单个失败只记一行日志，不重试、不阻断同一轮其余动作，一个活动读失败也不影响另一个。抽签是唯一服务端状态挡不住重复的动作——`remaining_count` 到底是「今日剩余」还是「整个活动余额」没有证据，所以本地再按服务端日期记一次闩，且发出前先记，回包丢失也不补发。worker 内共用同一个自动化实例，否则每次定时器触发都会重置这个闩。

调度挂在 `startAutumnActivityTimer()`：登录后 75 秒首跑，之后每 30 分钟一次，全部经 `runExclusiveAutomationTask('autumn_activity', …)` 串行，保存设置时重新评估门控（关掉开关即撤表）。跳过原因按服务端日期去重，避免半小时一轮把「今天已领」刷满日志。

`core/tests/autumn-activities-auto.test.js` 用模拟服务验证：开关门控、每个键位都在配置白名单里（否则界面开关是死的）、`autumn_wish_choice` 不被布尔归一吃掉、领奖先于抽签并复用写回包状态、每服务端日期只发一发抽签、无效方向跳过而非猜测、已结算状态不发写、单项失败不阻断其余、读失败在活动间隔离、跳过日志按天去重、活动关闭时只报一次。合并后本地 229 项 core 测试、8 项 web 测试通过，core/client tsc、`vue-tsc` 与 Vite 生产构建通过。自动化尚未接真实账号验证。

## 验证边界

两账号的祈愿方向、会员状态和 SVIP 商品可见性对照已完成。快乐奖励的实际结果已确认，直接写入协议仍由官方编码向量与模拟服务测试覆盖。未进行他人点击快乐包的操作，未花费钻石，未发送 QQ 消息。
